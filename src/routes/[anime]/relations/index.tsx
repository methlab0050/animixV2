import { component$ } from "@builder.io/qwik";

export default component$(()=>{
    return <> 
        This is for relations between series, see <a href="https://chiaki.site/?/tools/watch_order">chiaki</a> for a good example of what this could look like 
    </>
});